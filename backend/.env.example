# Usado apenas para desenvolvimento local SEM Docker (manage.py runserver)
# Para Docker, as variáveis vêm do docker-compose via .env na raiz do projeto

SECRET_KEY=django-insecure-local-dev-key-shadow-finance-2026
DEBUG=True
DJANGO_ENV=development
